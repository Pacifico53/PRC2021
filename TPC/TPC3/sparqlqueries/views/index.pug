extends layout
block content
    .w3-card-4
      header.w3-container.w3-teal
        h3 Lista de Repositorios
      if lista.length > 0
        .w3-container
          table(class="w3-table-all")
            tr
              th ID
              th Titulo
            each a in lista
              tr
                td
                  a(href='/rep/'+a.id) #{a.id}
                td= a.tit
      else
        div(class="w3-container")
          p Ainda não há repositorios...
      footer(class="w3-container w3-teal")
        address Gerado por BVC :: 2021-Março